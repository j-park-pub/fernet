% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encrypt.R
\name{encrypt}
\alias{encrypt}
\title{Encrypt data using Fernet}
\usage{
encrypt(key, data)
}
\arguments{
\item{key}{A character string containing a URL-safe base64-encoded Fernet
key, as produced by \code{\link[=generate_key]{generate_key()}}. Must decode to 32 bytes.}

\item{data}{A raw vector (or object coercible to one) containing the
plaintext data to encrypt.}
}
\value{
A character string containing the URL-safe base64-encoded Fernet
token.
}
\description{
Encrypts a message using the Fernet symmetric encryption scheme, producing
a URL-safe base64-encoded token that includes a version byte, timestamp,
initialization vector, AES-CBC encrypted ciphertext, and an HMAC signature.
}
\examples{
key <- generate_key()
encrypt(key, "hello world")
}
