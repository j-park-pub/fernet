% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decrypt.R
\name{decrypt}
\alias{decrypt}
\title{Decrypt a Fernet token}
\usage{
decrypt(key, token, ttl = NULL)
}
\arguments{
\item{key}{A character string containing a URL-safe base64-encoded Fernet
key, as produced by \code{\link[=generate_key]{generate_key()}}. Must decode to 32 bytes.}

\item{token}{A character string containing a URL-safe base64-encoded
Fernet token, as produced by \code{\link[=encrypt]{encrypt()}}.}

\item{ttl}{Optional numeric time-to-live, in seconds. If supplied, the
token's embedded timestamp is checked and an error is raised if the
token has expired.}
}
\value{
A character string containing the decrypted plaintext message.
}
\description{
Decrypts a Fernet token, verifying its version byte and HMAC signature
before decrypting the ciphertext with AES-CBC and removing PKCS7 padding.
}
\examples{
key <- generate_key()
token <- encrypt(key, "hello world")
decrypt(key, token)
}
