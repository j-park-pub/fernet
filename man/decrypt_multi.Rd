% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decrypt_multi.R
\name{decrypt_multi}
\alias{decrypt_multi}
\title{Decrypt a token trying multiple keys}
\usage{
decrypt_multi(keys, token, ttl = NULL)
}
\arguments{
\item{keys}{A character vector of base64url-encoded 256-bit Fernet keys,
tried in order until one successfully decrypts \code{token}.}

\item{token}{A character string containing the base64url-encoded Fernet
token to decrypt.}

\item{ttl}{Optional time-to-live in seconds. If supplied, decryption
fails if the token is older than \code{ttl} seconds. Passed through to
\code{\link[=decrypt]{decrypt()}}.}
}
\value{
A character string containing the decrypted plaintext.
}
\description{
Attempts to decrypt a Fernet token using each key in \code{keys}, in order,
returning the plaintext from the first key that succeeds. This supports
key rotation: pass the current key(s) alongside retired ones so that
tokens encrypted under an older key can still be decrypted.
}
\examples{
old_key <- generate_key()
new_key <- generate_key()
token <- encrypt(old_key, "hello world")
decrypt_multi(c(new_key, old_key), token)
}
