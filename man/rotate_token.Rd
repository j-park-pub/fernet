% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotate_key.R
\name{rotate_token}
\alias{rotate_token}
\title{Re-encrypt a token under a new key}
\usage{
rotate_token(old_keys, new_key, token, ttl = NULL)
}
\arguments{
\item{old_keys}{A character vector of base64url-encoded 256-bit Fernet
keys to try when decrypting \code{token}, in order.}

\item{new_key}{A base64url-encoded 256-bit Fernet key to encrypt the
recovered plaintext with.}

\item{token}{A character string containing the base64url-encoded Fernet
token to rotate.}

\item{ttl}{Optional time-to-live in seconds, passed through to
\code{\link[=decrypt_multi]{decrypt_multi()}} when decrypting \code{token}.}
}
\value{
A character string containing the base64url-encoded Fernet token,
encrypted under \code{new_key}.
}
\description{
Decrypts \code{token} using \code{\link[=decrypt_multi]{decrypt_multi()}} against one or more candidate
\code{old_keys}, then re-encrypts the resulting plaintext with \code{new_key}. This
supports key rotation: existing tokens can be migrated to a new key
without ever needing to store the plaintext.
}
\examples{
old_key <- generate_key()
new_key <- generate_key()
old_keys <- c(old_key, generate_key(), generate_key())
token <- encrypt(old_key, "hello world")
rotate_token(old_keys, new_key, token)
}
